The file structure looks somewhat like this:
~
  -proj_django
    -settings.py
    -urls.py
  -static
    -css
      -main.css
  -vec_esports
    -templates
      -guestbook
        -main_page.html
      -__init__.py
      -models.py
      -urls.py
      -views.py
  -main.py
  -manage.py

proj_django provides all the 'base' django setup.
  This folder is mostly boilerplate that connects to the actual app and its configuration files.
  settings.py is the typical django settings file.
    ADMIN
    INSTALLED_APPS
  urls.py connects to vec_esports\urls.py
static contains static files
vec_esports contains the projects main files


Add   login: required    to a handler>url in app.yaml to restrict a page to only logged in users.
